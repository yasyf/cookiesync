{"v":1,"lamport":3,"ops":[{"kind":"set_title","title":"`ab` is the ONE agent-browser entry point — routes Browserbase vs local by env var; never hand-roll raw agent-browser calls in the abwc skill"},{"kind":"set_body","body":"`ab` (`bin/ab` in the agent-browser-with-cookies plugin) is the **universal** wrapper for every agent-browser call in that skill. It routes to Browserbase when `BROWSERBASE_API_KEY` (or the 1Password fallback) resolves, else to the local default (Clark); `--local` forces local. The skill must drive EVERY call through `\"$ab\"` — open, seed, verify, task, cleanup.\n\n**Why:** the user was emphatic (\"it should ALWAYS use ab, which routes to local default or browserbase based on the env var — we've been over this already\"). skill 0.10.0 had drifted: it documented `ab` as Browserbase-only and told agents to hand-roll a raw `agent-browser --session abwc` + FIFO recipe for local mode. A getaway gatherer followed that prose literally and wedged the daemon. Do NOT reintroduce raw `agent-browser` invocations into the skill prose — local recovery paths included (use `\"$ab\" --local …`).\n\n**How to apply:** abwc 0.11.0 (cc-skills 7d640c9) made this real — `ab mode` prints the resolved backend, `bin/abwc-seed` resolves the backend ONCE at entry and pins every child call to it (local → `--local` on every call; browserbase → `export BROWSERBASE_API_KEY` so no child re-resolves). When editing this skill: one `ab` path, mode resolved once, no per-call backend re-resolution that could split a flow across two backends. See [[cookiesync-web-storage]] for the 0.11.0 seeding-helper details and [[agent-browser-state-handoff]] for the FIFO mechanics.\n\n**bin/ab session lifecycle (0.12.1 hardening, 2026-07-13):** Browserbase exposes NO idempotency key (confirmed against their API ref — their own retry example just re-`create()`s), so `ab` enforces ≤1 live session per caller in code: slug = `ab-\u003csanitize\u003e-\u003cshasum-256 8-hex of full TAG\u003e` (the old `tr -C 'A-Za-z0-9' '-'` sanitizer was non-injective → distinct requestors shared one browser); a `mktemp` marker file is armed immediately after the create POST so a kill before the id is read still releases it; the create POST drops `--retry` and adopts an existing tagged session on failure rather than duplicating; and reap-others-to-newest runs ONLY in `ab close`, never on the live path — a live-path reap can release the session the daemon is actively using (codex killed that twice, HIGH — the danger is real under sequential-with-duplicates AND same-TAG concurrency). `find_session`/`ab mode`/`--local` routing UNCHANGED."}]}