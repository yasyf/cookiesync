{"v":1,"lamport":8,"ops":[{"kind":"append_entry","text":"macOS forensics from a fish shell: `log show --predicate ...` silently runs fish's BUILTIN `log`, not /usr/bin/log — you get baffling argument errors instead of unified-log output. Always invoke it as `command log show ...` (or /usr/bin/log) in fish. Cost ~10 minutes of the Arc cookie-clobber investigation before the shadowing was spotted."}]}