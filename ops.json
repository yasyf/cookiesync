{"v":1,"lamport":18,"ops":[{"kind":"set_title","title":"cookiesync captures localStorage/sessionStorage (not just cookies) for localStorage-auth sites like Faye; Browserbase needs -p on EVERY call"},{"kind":"set_body","body":"cookiesync now captures **web storage** (localStorage + sessionStorage) alongside cookies so localStorage-auth sites (e.g. Faye / app.findfaye.com — token lives in localStorage key `auth`, NOT a cookie, NOT IndexedDB) drive logged-in in `agent-browser-with-cookies`. Shipped 2026-07-07.\n\n- **cookiesync (Go, committed a2f949bd, CI green):** new `internal/cookie/webstorage.go` goleveldb reader (copy-first, LOCK-safe, Chrome String16 marker-byte decode 0x00=UTF-16LE/0x01=Latin-1). `--format playwright` now emits real `origins[].localStorage`; new `--format webstorage` sidecar also carries sessionStorage (Playwright storageState has no sessionStorage slot). New consent-gated LOCAL-only `get_web_storage` RPC reuses the get_cookies grant (no extra tap; key discarded — web storage is unencrypted). **IndexedDB deferred**: goleveldb can't open it (custom `idb_cmp1` comparator + V8 structured-clone values).\n- **skill agent-browser-with-cookies 0.8.0 (cc-skills, committed 1bc63e1e):** local `state load` restores localStorage automatically (no command change). Browserbase seeds per-origin via `storage local/session set` + reload.\n- **Browserbase reuse FIXED via `ab` (skill 0.9.0, cc-skills, SHIPPED).** Root cause: agent-browser's built-in `-p browserbase` creates a NEW cloud session per CLI call, no reconnect → follow-ups land on `about:blank`. `ab` (`bin/ab`, replaces `agent-browser-bb`) keeps ONE keepAlive Browserbase session per caller: tag = `cookiesync requestor` slug (userMetadata.ab), found via `GET /sessions?status=RUNNING\u0026q=user_metadata['ab']:'\u003cslug\u003e'`, reconnected with **`agent-browser connect \"\u003cconnectUrl\u003e\"`** (NOT `--cdp` — that's port-only; re-connect resets the page so connect only when `agent-browser session list` lacks the slug), released on `ab close` via `POST /sessions/{id} {status:REQUEST_RELEASE}`. Key facts: create needs NO projectId (default project); keepAlive REQUIRED (paid plan) or session dies on disconnect; userMetadata values must be alnum/dash (spaces/`·` → 400, so use the SLUG not the display tag); connectUrl comes from the API (list omits it, GET /sessions/{id} has it), never construct `wss://connect.\u003cregion\u003e`; Browserbase DOES have per-plan concurrent caps (Free 3/Dev 25). ab feeds the key to curl via stdin `--config -` (not argv), and a release-trap frees a just-created session if connect fails. Live-verified: reuse, single session, close-releases, orphan-release, Faye→/home on cloud IP. Chose cdp-wrapper over an agent-browser browser.provider PLUGIN (which IS a real capability, but wrapper is simpler). Also NEW: **`cookiesync requestor`** CLI (in v0.10.0) prints the stable requestor token (pid fallback) — the single identity source ab reuses.\n- **cookiesync v0.10.0 RELEASED** (2026-07-08). Carries `get_web_storage` + `cookiesync requestor`.\n- **Stale-daemon incident RESOLVED (2026-07-10, v0.10.1):** the real gap was never a missing upgrade — brew cask upgrades swap the binary but never touch launchd, so yasyf-home's `helper-serve` kept running pre-0.10.0 code for two days after the 0.10.0 upgrade and rejected `get_web_storage` (`cookiesync: unknown method`). Diagnosis key: that error means the RPC round-tripped to a live daemon whose dispatcher lacks the method — check the daemon process's `lstart` against the Caskroom install time; `cookiesync doctor` shows all-green for a stale daemon (no version/capability check, declined as out of scope). **v0.10.1 SHIPPED + live-verified on both Macs:** the cask now has a goreleaser-generated `postflight` (`hooks.post.install` in .goreleaser.yaml — never hand-edit the tap file) running `launchctl kickstart -k gui/#{Process.uid}/com.github.yasyf.synckit.helper.cookiesync`, `must_succeed: false` so first-install/headless are quiet no-ops; upgrades now self-deploy (helper lstart moved to the upgrade moment on both Macs with zero manual launchctl). Every helper restart drops the warm key cache → one fresh routed tap on next use. abwc recipe E2E re-verified on yasyf-home (Apple developer.apple.com/account signed in).\n\n- **v0.10.2 RELEASED (2026-07-10):** cask postflight now also strips `com.apple.quarantine` (`xattr -dr` before the kickstart) — cookiesync was the one bare-binary cask in the fleet missing the scaffold-default strip. Upgraded + live-verified on this Mac; yasyf-home picks it up on its next brew upgrade.\n\n- **IndexedDB is DEFERRED, not \"airline/token auth\" (2026-07-12):** a getaway refresh session that hung on a cookie pull mislabeled a logged-out airline landing as \"IndexedDB auth\" and wrote it into `getaway/skills/refresh/gather.md`. RETRACTED (getaway 0.7.0) — the line is now neutral (\"logged-out after a verified-fresh login → note and skip\"). Only claim IndexedDB when a real test demonstrates it. What cookiesync captures: cookies + localStorage + sessionStorage; IndexedDB stays out (idb_cmp1 + V8 values). See [[cookiesync-union-hang-and-deadline-fixes]] for the actual root cause of that hung session.\n\n- **abwc skill 0.11.0 SHIPPED (2026-07-12, cc-skills 7d640c9):** `ab` is now the UNIVERSAL entry point (see [[ab-universal-router]]); seeding moved out of copy-paste FIFO prose into shipped `bin/abwc-seed \u003chosts\u003e` — a bounded FIFO rendezvous that can't wedge the agent-browser daemon, `--format X` BEFORE the `--` host terminator (a `--` before `--format` silently swallows the format → default playwright; verified against the real cookiesync CLI), once-resolved backend pinning, base64-safe storage bridge, per-step Browserbase error accumulation. Three adversarial review rounds. Installed plugin cache refreshes to 0.11.0 on next `claude` restart (was 0.10.0). agent-browser 0.31.1 KNOWN behavior: a `state load` that stays pending \u003e~60s or whose client is killed wedges that PER-SESSION daemon (session list still answers) — documented pkill recovery in SKILL.\n\n- **abwc skill 0.12.0 SHIPPED (2026-07-13, cc-skills 1be0b1f):** per-agent `--session \u003cname\u003e` on ab AND abwc-seed (`AB_SESSION` env fallback, flag wins; dangling/empty value errors). The override SUFFIXES the requestor-derived TAG before slugification — grant stays requestor-keyed (one tap per Claude session), only the Browserbase tag + local session name fork, so parallel agents finally get isolated pages (live-verified: alpha/beta agents, one tap, per-slug close). Two seeding fixes rode along: Browserbase cookie seeding now opens each host and sets UNPINNED (a pinned `--domain` invalidates the whole CDP batch when a `__Host-` cookie is present — github.com); storage seeding is one `eval --stdin` per origin with entries as JSON (was one roundtrip per key — 1,484 keys/30+ min for github.com → ~19s). getaway gather.md doctrine updated same day (17dabcd1, ships with getaway's next release cut). KNOWN BUG (cookie-sync backlog task 3e458cf, FIXED in v0.10.5 — see below): `--format webstorage` emitted a malformed glued origin `https://github.com/^0https://lemire.me`.\n\n- **abwc skill 0.12.1 SHIPPED (2026-07-13, cc-skills):** `bin/ab` session-lifecycle hardening — see [[ab-universal-router]] for the design. Browserbase has NO idempotency key, so `ab` enforces ≤1 live session per caller in code: collision-resistant slug (`ab-\u003csanitize\u003e-\u003cshasum-256 8-hex of full TAG\u003e`; the old `tr -C 'A-Za-z0-9' '-'` sanitizer was non-injective — `a/b` and `a?b` collided → shared browser), a `mktemp` marker file armed right after the create POST (before the id is read, closing the kill-between-POST-and-arm leak), a NO-`--retry` create POST that adopts an existing tagged session on failure instead of duplicating, and reap-to-newest ONLY inside `ab close` (a live-path reap could release the daemon's in-use session — codex flagged HIGH twice). cc-skills backlog task 3e458cf (malformed webstorage origin) still open.\n\nRelated: [[cookiesync-architecture]], [[agent-browser-state-handoff]], [[cookiesync-consent-routing-unified]], [[cookiesync-union-hang-and-deadline-fixes]], [[ab-universal-router]], [[claude-plugin-cache-local-dev]]."}]}